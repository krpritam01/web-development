// Implementation

// Function Returning Value

// Declaration 

function passMessage() {
    // function Body

    return "I am to Learn";
}

// Calling a Function

let message = passMessage();

//The function would be excuted and on exeuting the return statement the value / expression are stored in the return_value variable

console.log(message);